<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.roadscanner.dao.PostDAO">
    <select id="getAllPosts" resultType="com.roadscanner.dto.Posts">
        SELECT * FROM POSTS
    </select>

    <select id="getPostById" parameterType="Long" resultType="com.roadscanner.dto.Posts">
        SELECT * FROM POSTS WHERE ID=#{id}
    </select>

    <insert id="createPost" parameterType="com.roadscanner.dto.Posts">
        INSERT INTO POSTS (CATEGORY, TITLE, CONTENT, AUTHOR, CREATED_DATE, UPDATED_DATE, VIEWS)
        VALUES (#{category}, #{title}, #{content}, #{author}, #{createdDate}, #{updatedDate}, #{views})
        <selectKey keyProperty="id" resultType="Long" order="AFTER">
            SELECT POST_SEQ.CURRVAL FROM DUAL
        </selectKey>
    </insert>


    <delete id="deletePost" parameterType="Long">
        DELETE FROM POSTS WHERE ID=#{id}
    </delete>

    <update id="updatePost" parameterType="com.roadscanner.dto.Posts">
        UPDATE POSTS
        SET CATEGORY=#{category}, TITLE=#{title}, CONTENT=#{content}, AUTHOR=#{author}, UPDATED_DATE=#{updatedDate}, VIEWS=#{views}
        WHERE ID=#{id}
    </update>
</mapper>